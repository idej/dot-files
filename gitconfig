[user]
  name  = idej
  email = idej.wrk@gmail.com
[core]
  pager = less -FRSX
  whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
  editor = vim -f
  autocrlf = input
  safecrlf = true
  excludesfile = /Users/idej/.gitignore
[advice]
  statusHints = false
[web]
  browser = open
[push]
  config = default
  default = tracking
[rebase]
  autosquash = true
[merge]
  summary = true
  tool = meld
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
[format]
  pretty = %C(yellow)%h%Creset %s %C(red)(%an, %cr)%Creset
[branch "master"]
  remote = origin
  merge = refs/heads/master
[filter "media"]
  required = true
  clean = git media clean %f
  smudge = git media smudge %f
[mergetool]
  keepBackup = true
[filter "lfs"]
  clean = git-lfs clean %f
  smudge = git-lfs smudge %f
  required = true
